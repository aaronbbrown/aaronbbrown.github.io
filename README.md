# Prerequisites

* Mac OSX
* rvm
* boot2docker (running!)
```
boot2docker download
boot2docker init
boot2docker up
```

# To build the Docker container

```
./build.sh
```

# To run the Docker container

```
./run.sh
```
